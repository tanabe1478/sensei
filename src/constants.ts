/** Discord メッセージの最大文字数 */
export const DISCORD_MAX_LENGTH = 2000;

/** 安全マージンを取った送信上限 */
export const DISCORD_SAFE_LENGTH = 1900;

/** エージェント実行のデフォルトタイムアウト (5分) */
export const DEFAULT_TIMEOUT_MS = 5 * 60 * 1000;

/** アイドルプロセスのデフォルトタイムアウト (30分) */
export const DEFAULT_IDLE_TIMEOUT_MS = 30 * 60 * 1000;

/** ストリーミング更新の最小間隔 */
export const STREAM_UPDATE_INTERVAL_MS = 1500;

/** sensei のデータディレクトリ名 */
export const DATA_DIR_NAME = '.sensei';
